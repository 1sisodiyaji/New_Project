const email = document.getElementById('loginName');
// first check weather the email id is registererd or not

const pass = document.getElementById('loginPassword');

function Login(){
    if(!email){
        
    }
  
}